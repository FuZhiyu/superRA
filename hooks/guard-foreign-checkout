#!/usr/bin/env bash
# PreToolUse gate: mutating a task tree in a foreign checkout needs approval.

case "$0" in
  */*) script_dir=${0%/*} ;;
  *) script_dir=. ;;
esac
gate="checkout_isolation_gate.py"
. "$script_dir/run-python-gate"
