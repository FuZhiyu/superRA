---
name: data-exploration
description: Deprecated — folded into superRA:planning-workflow as Phase 1 (Data Inventory hard gate). Invoke superRA:planning-workflow instead. This stub will be removed once cross-references migrate.
---

# Data Exploration (deprecated)

The data-exploration skill has been folded into `superRA:planning-workflow` as **Phase 1: Data Inventory** with the same `<HARD-GATE>` enforcement. Invoke `superRA:planning-workflow` directly — Phase 1 handles data inventory and Phase 2 handles plan creation in one continuous workflow.

This stub exists temporarily to redirect anything still pointing at `superRA:data-exploration`. It will be deleted once all cross-references have been updated to invoke `superRA:planning-workflow`.
