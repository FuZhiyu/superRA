---
name: pre-merge-gate
description: Deprecated — renamed to superRA:integration-workflow. Invoke that instead. The renamed skill also absorbed the report generation and development-document handling steps that used to live in finishing-analysis Steps 4b/4c. This stub will be removed once cross-references migrate.
---

# Pre-Merge Gate (deprecated)

The `pre-merge-gate` skill has been renamed to `superRA:integration-workflow` as part of the workflow/utility split. The renamed skill also absorbed Steps 4b (Generate Report) and 4c (Handle Development Documents) from the former finishing-analysis, so the integration phase is now a single workflow skill that produces a merge-ready branch.

Invoke `superRA:integration-workflow` directly. This stub exists temporarily and will be deleted once all cross-references have been updated.
