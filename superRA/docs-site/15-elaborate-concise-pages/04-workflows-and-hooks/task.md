---
title: "Workflows + Hooks: Elaborate Plan/Implement/Integrate Pages and Drop Cursor"
status: approved
depends_on: []
tags: []
created: 2026-06-18
---

## Objective

**Second pass (depth).** The first-pass record below resolved every marker but read too terse — the integrate page's five stage labels are the clearest case. Redo these pages to the parent's [Revision standard](../task.md), matching the worktree-data-sync reference — read its before/after at commit `db54356c` (`git show db54356c`) first. For each design idea (placement/consolidation on plan; the five integrate stages) give the *why* and a concrete picture an adopter can trust, lead with the user-facing concept, and explain any unfamiliar term plainly — without re-narrating the linked skill's internal steps. Supersede the first-pass `## Results` with the second-pass account.

Resolve the researcher's markers on the three workflow pages and the hooks page, grounding design-idea claims in the owning skills.

**[01-plan](../../../../docs/site/05-workflows/01-plan/task.md).** Marker: *"explain the placement principle, and explain the ex-post consolidation etc"*. Add a short adopter-level passage on two ideas the condensing dropped: how superRA decides where new work goes — concern-first placement, reading task objectives to nest work under the matching workstream rather than by title or by creating a fresh tree — and the ex-post consolidation pass, where a tree that has accrued structural debt gets merged/restructured rather than piled onto. Ground in `skills/superplan/references/task-tree-design.md` (§Placing Work by Durable Home) and `references/consolidation.md`. Keep it conceptual, not a procedure dump.

**[02-implement](../../../../docs/site/05-workflows/02-implement/task.md).** Fix the broken prose under `### Direct vs. subagent mode`. The WIP left an ungrammatical sentence: *"In **Direct mode**, where the main agent implements the task itself, is available for small, clearly-scoped tasks..."*. Repair it into a clean two-mode contrast (subagent mode is the default; Direct mode is for small clearly-scoped tasks; review is never skipped either way). The WIP also deleted the `### Resuming a project` section — treat that as the researcher's intentional cut and do not restore it; if you judge the frontier/resume idea genuinely valuable for adopters, flag it to the orchestrator rather than silently re-adding.

**[03-integrate](../../../../docs/site/05-workflows/03-integrate/task.md).** This is the clearest too-terse case — the five stages are a one-line numbered list. Redo it to the depth bar: **give each stage its own `###` subsection** (Protect / Sync / Refactor / Document / Finish), not a one-liner, and in each explain the concept an adopter needs, not just the label. Concretely: **Protect** = pin key results with drift/regression tests so a later change that moves a number fails loudly instead of slipping through (define what a drift test is). **Sync** = explain what the *base branch* is (the shared branch your work will land on — e.g. `main`/the trunk — which others have advanced while you worked) and that Sync folds those incoming changes in *by intent* (semantic merge), not by mechanically resolving each textual conflict. **Refactor** = explain the **minimum-net-diff** principle against that base: the surviving change is the smallest reviewable diff that does the job, fitting local conventions and reusing what is there, rather than a pile of one-shot scripts. **Document** = mature each `task.md`'s findings from a working dev log into durable documentation a future reader can follow. **Finish** = re-check the base has not moved again, then ship by PR or fast-forward and clean up. The WIP removed the "It is a separate phase rather than a final `git commit`" lead-in and the `### When can I skip INTEGRATE?` section — treat both as intentional cuts. Ground in `skills/superintegrate/SKILL.md` (Sync §base resolution, Integrate), `result-protection`, and `refactor-and-integrate` (minimum-net-diff).

**[06-hooks](../../../../docs/site/06-hooks/task.md).** Marker: *"no Cursor support. drop it."*. Remove the Cursor column from the hook table and the Cursor wiring reference (`hooks/hooks-cursor.json`) from the page. Check `hooks/` to confirm what is actually shipped so the page does not advertise support that is not there or drop wiring that is.

Success: all markers removed; the integrate stages each convey their design idea; the implement prose is grammatical; the hooks page no longer claims Cursor support; pages stay within the agent-first / non-obvious-only / no-AI-prose gates.

## Planner Guidance

These pages link to the owning workflow skills as the authority — elaborate the *why* of each phase/stage enough for a reader to trust it, but keep the step-level mechanics in the linked `SKILL.md`. The integrate page is the clearest case of condensing-gone-too-far: the five labels without their rationale read as a bare list.

## Results

**Second pass (depth).** Redone to the parent's [Revision standard](../task.md) against the worktree-data-sync reference (`db54356c`). The first pass had resolved every marker (committed `c7ea0333`) but the integrate page's five stages stayed a one-line numbered list — the clearest too-terse case. This pass converts that list into per-stage `###` subsections at the reference depth; the other three pages already met the depth bar in the first pass and were left as-is. All pages stay within the agent-first / non-obvious-only / no-AI-prose gates (no banned hype nouns, antithesis, or empty intensifiers; `report-in-markdown` self-diagnose clean on the integrate page).

- **03-integrate** (reworked this pass) — Replaced the numbered one-liner list with six `###` subsections, each leading with the concept and stating why it matters. Protect: defines a *drift test* in plain language (a check that records a result's value and fails when a later step moves it — like a unit test, but guarding a finding), with concrete pinned-value examples, plus the *why* (quiet regressions reach the paper unnoticed otherwise). Sync: defines the *base branch* (the shared trunk your work lands on, which others advanced while you worked) and contrasts intent-based reconciliation against a plain textual `git merge`. Refactor: explains *minimum-net-diff* against the freshly synced base and why a tight reviewable diff matters. Document: dev-log → durable-record maturation, framed as editing for a reader who wasn't there. Finish: base-freshness recheck, then PR/fast-forward and worktree cleanup. A closing "what stays with you" subsection keeps the user-owned decisions. Grounded in `skills/superintegrate/SKILL.md` (Protect / Sync §base resolution / Integrate / Document / Finish), `result-protection` (drift tests as default mechanism), and `refactor-and-integrate` §Minimum Net Diff.
- **01-plan** (unchanged this pass) — First-pass placement/consolidation passages already read at the depth bar: concern-first placement (new work nests under the workstream whose objective owns the concern, not by title/recency; a new root task is the exception) and the ex-post consolidation pass (whole-tree merge/prune/restructure, propose → approve → single commit). Grounded in `skills/superplan/references/task-tree-design.md` §Placing Work by Durable Home and `references/consolidation.md`.
- **02-implement** (unchanged this pass) — First-pass repair of the ungrammatical `### Direct vs. subagent mode` sentence already reads as a clean two-mode contrast (subagent default; Direct mode for small clearly-scoped tasks; reviewer dispatched either way) at the depth bar. The `### Resuming a project` cut stays in place as intentional; the resume idea is already covered upstream on 01-plan, so no callout was re-added.
- **06-hooks** (unchanged this pass) — Cursor column, coverage-note line, and `hooks/hooks-cursor.json` wiring reference already dropped in the first pass. Re-verified against `hooks/` that Claude Code (`hooks.json`) and Codex (`hooks-codex.json`) are the only wired harnesses; the surviving Claude/Codex columns are accurate.
