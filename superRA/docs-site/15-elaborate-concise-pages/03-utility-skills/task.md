---
title: "Utility Skills: Elaborate Task-Tree, Worktree-Data-Sync, Zotero, and Mistral Pages"
status: approved
depends_on: []
tags: []
created: 2026-06-18
---

## Objective

**Second pass (depth).** The first-pass record below resolved every marker but read too terse for a new adopter. Redo these pages to the parent's [Revision standard](../task.md): explain unfamiliar concepts in plain language (symlink/COW were the reference case), lead with the user-facing choice and show it as the prompt you give the agent, give a worked example for any vague mechanism, and say why each choice matters. [06-worktree-data-sync](../../../../docs/site/04-utility-skills/06-worktree-data-sync/task.md) is the worked reference and is **already done** — read its before/after at commit `db54356c` (`git show db54356c`), then redo the other three pages (task-tree, zotero-paper-reader, mistral-pdf-to-markdown) to that bar. Supersede the first-pass `## Results` with the second-pass account.

Resolve the researcher's markers on the four utility-skill pages, grounding every behavior claim in the owning `SKILL.md` and its scripts — do not invent flags, modes, or env-var names.

**[01-task-tree](../../../../docs/site/04-utility-skills/01-task-tree/task.md).** The WIP moved the `## What the agent runs, and what you can run yourself` section up and left rough prose (*"If it is not setup, request agent to set up the CLI tool."*, *"Agents edit regularly, while hooks ensure the integrity of the trees."*) and commented out the direct-edit-vs-CLI sentence. Polish into clean prose: the agent drives the tree through the committed `./superRA/superra` wrapper and you can run the same commands to inspect or steer; the agent bootstraps the wrapper if it is missing; agents edit task files continuously while hooks validate the tree and propagate the rollup. Decide whether to restore the commented-out detail (setting one field is a direct `task.md` edit; use the CLI for structural ops like `task move`, never raw `mv`) in cleaner form or leave it cut — keep it if it closes a real reader gap, drop it if it re-narrates mechanics the linked subpages own. Ground in `skills/task-tree/SKILL.md`.

**[06-worktree-data-sync](../../../../docs/site/04-utility-skills/06-worktree-data-sync/task.md).** Two markers: *"explain two modes of data sync: symlink vs COW (only available for mac). explain the default and the .gitignore annotation mechanism"* and *"explain how to sync the data back"*. Read `skills/worktree-data-sync/SKILL.md` and `scripts/sync_worktree_data.py` and state accurately: the two seed modes (symlink vs copy-on-write clone, with COW being macOS-only) and which is the default; the `.gitignore` annotation mechanism that marks what counts as syncable data; and how results are synced back (the reconcile path). Confirm the actual flag/mode names before writing.

**[07-zotero-paper-reader](../../../../docs/site/04-utility-skills/07-zotero-paper-reader/task.md).** Two markers: *"add link to better bibtex. and explain we need better bibtex for bib management. elaborate a bit."* — add a link to the Better BibTeX project and one or two sentences on why it is needed (stable, deterministic citekeys that match your master `.bib`). And on the renamed `**Export bib reference.**` bullet, *"this is unclear how to use. explain a bit more on how to use this skill."* — the WIP left this split across lines and choppy; rewrite it into a clean account of adding an entry to `refs.bib` (dedup-append so re-running never double-adds), dropping the cite at a `--marker` placeholder, and exporting BibTeX / formatted references. Ground in `skills/zotero-paper-reader/SKILL.md`.

**[08-mistral-pdf-to-markdown](../../../../docs/site/04-utility-skills/08-mistral-pdf-to-markdown/task.md).** Marker: *"explain the API, where to get one, where to save it, free tier usage etc."* — add a short setup note: it calls the Mistral OCR API, so it needs an API key (where to obtain it, and the free-tier availability), and where the key is read from. Read `skills/mistral-pdf-to-markdown/SKILL.md` for the exact env-var name / key location and use that — do not guess.

Success: all markers removed; data-sync modes, the Mistral key location, and Better BibTeX described accurately against source; pages stay within the agent-first / non-obvious-only / no-AI-prose gates.

## Planner Guidance

The task-tree page is the conceptual top of its subtree — keep the operational command detail pointing at `01-task-tree/02-cli-commands` rather than re-listing it here. For data-sync and Mistral, the setup facts (modes, key) are exactly the non-obvious detail worth spending words on; that is the gap the condensing dropped.

## Results

**Second pass (depth).** The first pass (`4e23affe`) resolved every marker but read too terse for a new adopter; the parent then redid 06-worktree-data-sync to the depth bar at `db54356c`. This pass redoes the remaining two pages that fell short of that bar (task-tree, zotero) and confirms the third (mistral) already meets it. No `<!-- -->` markers remain on any of the four pages; both edited pages pass `check_markdown.py` clean.

**01-task-tree.** Broke the dense two-paragraph `## What the agent runs` section into four scannable `###` subsections, one idea each, to the reference bar. Defined the terms an adopter may not know in plain language at first use: **hook** ("a check git runs automatically on each change"), **rollup** ("a parent task's status is computed from its children, never set by hand"). Made the by-hand-edit-vs-CLI distinction concrete with worked examples on each side (a status flip / typo fix is a direct `task.md` edit; `task move` carries the directory and rewrites dependency links where a raw `mv` would leave them dangling), and stated why the choice matters (the dangle is silent — the tree still compiles). Kept this distinction at the conceptual top since the linked `02-cli-commands` subpage owns the command surface but not this hand-edit-vs-structural-op rule. Command detail stays pointed at the subpages per planner guidance. Grounded in `skills/task-tree/SKILL.md`.

**07-zotero-paper-reader.** Promoted the Better BibTeX explanation to a `### Why Better BibTeX` subsection and led with the concept an adopter needs first: defined **citekey** in plain language ("the short label you cite a paper by — the `key` in `\cite{key}`") and stated the concrete consequence Better BibTeX prevents (Zotero's built-in export assigns unstable keys; the mismatch surfaces as a dangling reference at build time). Split the dense cite/bibtex paragraph into four scannable bold bullets under `### Reading and citing` — read, add-to-`.bib` (dedup-append idempotence, with the why), cite-into-draft (`--marker` in-place replacement plus the missing-marker safety), and export-without-a-draft — and defined **CSL style** in plain language ("Citation Style Language, the standard that defines how a reference reads in APA, Chicago…"). Grounded in `skills/zotero-paper-reader/SKILL.md` §Citations & BibTeX and `references/bibtex-citations.md` (§Key model, `cite`, §Master-`.bib` sync semantics, §Selection flags / `bibliography`).

**08-mistral-pdf-to-markdown.** Already at the depth bar from the first pass and left unchanged: it leads with the user-facing decision (what to convert and the page-range cost lever), defines the failure mode plainly (a scan has no text layer; two-column text reads straight across), and carries a `### The API key` subsection that states where to get a key ([Mistral console](https://console.mistral.ai/)), the free-tier note, and the exact key-lookup order (`MISTRAL_API_KEY` env → `paper-reader.mistral_api_key` in `agent-contract.yaml` → `Notes/.env`), all accurate against `skills/mistral-pdf-to-markdown/SKILL.md` §API Key Setup. Re-editing would only re-narrate.

**06-worktree-data-sync.** Done by the parent at `db54356c` (the worked reference); not touched this pass.
