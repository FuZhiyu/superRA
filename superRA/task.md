---
title: "Better Handoff"
status: in-progress
depends_on: []
tags: []
created: 2026-05-23
---

## Objective

Improve the superRA handoff system — task tracking, planning artifacts, and human-readable visualization.

## Conventions

Walk date: 2026-06-01.

`AGENTS.md` / `CLAUDE.md` at repo root is the contributor-facing authority for superRA internals. When modifying skills, hooks, agents, harness adapters, or internal docs, treat the work as skill creation: read owning files before editing, load `skill-creator` before editing `skills/*/SKILL.md`, keep changes focused, verify behavior rather than prose alone, preserve the README/user-facing vs AGENTS/internal separation, and apply the DRY + Necessity gate line by line. Ownership boundaries matter: workflow choreography belongs to `superplan` / `superimplement` / `superintegrate`; dispatch and reviewer-feedback adjudication belong to `agent-orchestration`; task anatomy belongs to `task-system/references/planning.md`; canonical role behavior belongs to `agents/implementer.md` and `agents/reviewer.md`; generated Codex/direct-mode artifacts must be regenerated from sources rather than hand-edited.

`README.md` is the user-facing product overview. It describes superRA as a PLAN -> IMPLEMENT -> INTEGRATE workflow with an implementer-reviewer pair, domain skills layered onto domain-neutral workflow phases, durable `superRA/` task files, and generated Codex named agents installed through `codex-superra-setup`. Keep product framing there; internal contributor rules stay in `AGENTS.md` / `CLAUDE.md`.

No additional `AGENTS.md`, `CLAUDE.md`, or `README.md` files were found under the touched implementation paths (`agents/`, `skills/task-system/`, `skills/superplan/`, `skills/using-superRA/`, `.codex/`, `superRA/review-planning-protocol/`). The walk intentionally did not load unrelated guidance under `skills/theory-modeling/`, `skills/writing/`, or `tests/claude-code/` because this task does not touch those directories.

## Integration Notes

- [task-system/task-root-rename](task-system/task-root-rename/task.md): researcher confirmed `better-handoff` as the Sync base for the task-root rename integration.
- [task-system/task-root-rename](task-system/task-root-rename/task.md): synced with `better-handoff` through `981715dbb01eb99085d664778a121ce1a0d09f2c` (merge `104d96f3`, sync-map commit `35ba610e`, sync review `083c2d20`), post-sync integration reviewed APPROVED at `1bfed861`, and final checks passed (`uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/test_worktree_selector.py skills/task-system/scripts/tests/test_state_preservation.py`, 369 passed; `uv run pytest skills/codex-superra-setup/scripts/test_sync_codex_agents.py`, 6 passed; `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`; `python3 skills/task-system/scripts/task_check.py`; `git diff --check 981715dbb01eb99085d664778a121ce1a0d09f2c..HEAD`).
- [task-system/planning-redesign/planmd-sweep](task-system/planning-redesign/planmd-sweep/task.md): researcher chose manual integration checks rather than drift/static tests for the protocol-level results; integration base for this worktree is `better-handoff`.
- [task-system/planning-redesign/planmd-sweep](task-system/planning-redesign/planmd-sweep/task.md): synced with `better-handoff` through `19457675bf570a079bc960765a1832736cddf918`, sync reviewed, integration reviewed, task-tree consolidation completed, and targeted tests passed (`uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/tests/test_state_preservation.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py`, 205 passed).
- [task-system/dashboard/view-navigation](task-system/dashboard/view-navigation/task.md): synced onto `better-handoff` through `aad548e35d8309d386ce9a6f68b1f4c820502276` (merge `018a4f88`), sync reviewed APPROVED, integration reviewed APPROVED; pruned the now-dead `Task.has_child_dependency_graph()` and added card-flow tests (244 passing).
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): merged `plan/codex-task-hooks` into `better-handoff` from base `66693ccf`. Conflict resolution kept the base branch's committed template-backed standalone dashboard exporter and applied the incoming `updated` metadata removal through the shared task data layer and `templates/summary_bar.html`; unrelated in-flight subtree Share/export work was stashed out before verification and is not part of this sync. `.plan/**/task.md` frontmatter now omits `updated`. Verification passed on a clean tree: `uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/tests/test_state_preservation.py` (288 passed) and `python3 skills/task-system/scripts/task_check.py --plan-root .plan`.
- [review-planning-protocol](review-planning-protocol/task.md): synced with `better-handoff` through `75a86ccebd0ec37a0a9e904f0fd5b824752cf6f1` (merge `1d14b1f1`), integration review initially requested DRY/Necessity and task-root evidence fixes (`865f5ed0`), fixes landed in `2afde1d2`, and narrow integration re-review approved at `59d1ee4`. Final validation passed: `uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/test_worktree_selector.py skills/task-system/scripts/tests/test_state_preservation.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py` (379 passed), `python3 skills/task-system/scripts/task_check.py --plan-root superRA`, `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`, and `git diff --check 75a86ccebd0ec37a0a9e904f0fd5b824752cf6f1..HEAD`.
- [review-planning-protocol](review-planning-protocol/task.md) + [task-system/cli-scripts/path-containment](task-system/cli-scripts/path-containment/task.md): PR #29 follow-up pass (task 05 recursive-context-conventions + path-containment) integrated onto `better-handoff`. Synced through `876178e32d22ef2643424a584809d782fce4dde9` (merge `76c2a9af`, Sync Map commits `48e5143b`/`7f4535c4`, sync review APPROVED `ae132223`); post-sync integration review APPROVED with no blocking findings. Checks passed: `~/.venv/bin/python -m pytest skills/task-system/scripts/test_task_system.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py` (216 passed), `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`, `python3 skills/task-system/scripts/task_check.py --plan-root superRA`.
- [integ-workflow](task-system/agent-interface/integ-workflow/task.md) (integrate-cleanup redesign, merged into integ-workflow 2026-06-01 per the update-task lifecycle): researcher confirmed Sync base = local `better-handoff` (`876178e3`, anchors `PRE_SYNC_BASE_SHA=75a86cce`, `BASE_HEAD_SHA=876178e3`) and scoped this integration to the `integrate-cleanup` subtree only — `review-planning-protocol` remains already-closed carried work and is not re-reviewed. Protect uses manual prose-coherence checks rather than drift tests, consistent with the protocol-level-results decision recorded above for `planmd-sweep`: the integrate-cleanup changes are skill/reference text with no numeric results to drift-test, and both children were already correctness-reviewed (`0eeda33a`, `afa70c6f`). Synced onto `better-handoff` via clean disjoint merge `014dbcec` (sync review APPROVED `728ab399`); post-sync integration review found one MAJOR (the gate verdict was being written to a temporary `## Revision Notes` slot that warns on approved tasks and is matured/removed) — relocated to the root durable integration record, fixed `11e1214b`, narrow re-review APPROVED `4e6a67ac`. Integration closed; targeted suite green (247 passed, `task_check` clean, `git diff --check` clean over `1d1efd0f..HEAD`). Consolidation Gate verdict: **clean-enough — pass skipped** (frontier = parent + 2 children, the one cross-edge `results-maturation-home`→`consolidation-gate` is declared; zero `consolidation.md §When to Consolidate` symptoms present).

## Sync Map

**Base branch:** `better-handoff`
**Pre-sync merge base:** `876178e32d22ef2643424a584809d782fce4dde9`
**Synced base head:** `fbefc8ee3bd84484f7d12f34015527f7f143b063`
**Incoming range:** `876178e32d22ef2643424a584809d782fce4dde9..fbefc8ee3bd84484f7d12f34015527f7f143b063`
**Sync commits:** `d74973558f7ee86d84f038c446801d722f465150`, `1590243fc8a261c7370baf3dc140b3f2521869b0`
**Sync review status:** `APPROVED`

### Branch Summary

**Incoming intent:** `better-handoff` advanced with the PR #29 follow-up pass — primarily a planning-redesign extension that splits the task contract into authoritative `## Objective` vs advisory `## Planner Guidance`, makes scoped context/conventions recursive (each convention lives on the lowest ancestor whose subtree it governs; `task_read.py` renders ancestor objectives), and refactors `planning.md` from "Root task.md Anatomy"/"Conventions Section" into recursive "Task Anatomy"/"Ancestor Context and Conventions"; plus `task_create.py --guidance`, a `path-containment` CLI hardening task, an `## Conventions` walk recorded on root task.md, a `superintegrate` H1 rename, and the migration of the nested `planning-redesign/review-planning-protocol/` subtree into a top-level `review-planning-protocol/` tree (rolling the parent to `approved` in the base's child set).

**Resolution thesis:** This is a real-overlap merge, not disjoint. The two sides edit the same files (`planning.md`, `superintegrate/SKILL.md`, root task.md, the planning-redesign parent) but with non-contradicting intent, so the governing resolution is synthesis: incoming's recursive-context refactor is the new structural baseline for `planning.md`, and our Stage-2 maturation-home redefinition + Section-Ownership/parent-rollup rewrites layer on top of it (the two are complementary — incoming redesigned task anatomy and the objective/guidance split; we redefined where matured results live). No intent-changing contradiction surfaced, so no escalation. The only invalidated incoming value was the planning-redesign parent status, corrected by rollup math.

### Sync Clusters

> **Sync cluster `planning-md-overlap` (2026-06-01):** commits `d7497355`; paths `skills/task-system/references/planning.md`; affects Tasks `task-system/agent-interface/integ-workflow`, `task-system/planning-redesign` (and its `review-planning-protocol`/`05-recursive-context-conventions` subtree).
> **Incoming intent:** Objective-vs-Planner-Guidance contract split, recursive scoped context/conventions, "Root task.md Anatomy"→"Task Anatomy" and "Conventions Section"→"Ancestor Context and Conventions" refactor, plus a `## Planner Guidance` deviation bullet in §Results Shape Stage 1.
> **Sync resolution:** git auto-merged. Incoming's recursive refactor is taken as the structural baseline; our §Results Shape Stage-2 highest-touched-task maturation-home redefinition and the Section-Ownership Orchestrator bullet + parent-rollup paragraph rewrites survived intact and ride on top. Both sides had independently added the identical `## Planner Guidance` concept in §Writing Objectives — deduplicated to one copy. No silent loss either side. The merge shifted line numbers, so the integ-workflow task's `planning.md` cross-references were re-anchored (`:167→:168`, `:200→:201`, `:203→:204`).
> **Integration context:** an Integrate reviewer reading integ-workflow's shipped `planning.md` diff will see our maturation redefinition interleaved with incoming's recursive-context refactor in the same file; both are intended.
> **User decision:** None.

> **Sync cluster `root-task-and-rollup` (2026-06-01):** commits `d7497355`; paths `superRA/task.md`, `superRA/task-system/planning-redesign/task.md`, `superRA/task-system/planning-redesign/review-planning-protocol/task.md` (deleted).
> **Incoming intent:** add the PR #29 follow-up §Integration Notes entry, flip the planning-redesign parent to `approved`, and finish migrating the nested `review-planning-protocol/` subtree to top-level (deleting the nested copy).
> **Sync resolution:** §Integration Notes conflict was a clean union — kept both incoming's PR #29 entry and our integ-workflow + consolidation-gate-verdict entry; the shared `## Conventions` block and the first five notes were identical and un-conflicted. Accepted the nested `review-planning-protocol/task.md` deletion (base intent; no dependents — nothing in our tree or skills references that path, and our new child has empty `depends_on`). Corrected the planning-redesign parent status `approved`→`in-progress`: incoming's `approved` was valid for the base child set, but union-merging our not-started `placement-and-update-lifecycle` child makes the rollup `in-progress` (confirmed by `task_check.py`, which would have failed on an `approved` parent with a not-started child).
> **Integration context:** None beyond the rollup correction.
> **User decision:** None.

> **Sync cluster `superintegrate-h1` (2026-06-01):** commits `d7497355`; paths `skills/superintegrate/SKILL.md`; affects Task `task-system/agent-interface/integ-workflow`.
> **Incoming intent:** rename the H1 from `# Integration Workflow` to `# superintegrate — the INTEGRATE phase`.
> **Sync resolution:** git auto-merged cleanly. Our `## Consolidation Gate`, de-rooted `## Document`, and rewritten §When to Lighten bullet are untouched and coherent; the H1 replacement did not shift line numbers below it, so the integ-workflow task's `SKILL.md` cross-references (`:242`, `:294`, `:358`) remain accurate.
> **Integration context:** None.
> **User decision:** None.

> **Sync cluster `shared-skill-and-codex-edits` (2026-06-01):** commits `d7497355`; paths `agents/*.md`, `.codex/agents/*.toml`, `skills/using-superRA/references/direct-mode-*.md`, `skills/agent-orchestration/SKILL.md`, `skills/superplan/SKILL.md`, `skills/superimplement/SKILL.md`, `skills/task-system/SKILL.md`, `skills/task-system/scripts/{task_create,task_read,test_task_system}.py`, several `skills/*/references/planning.md`.
> **Incoming intent:** the planning-redesign + PR #29 edits to role specs, generated Codex/direct-mode artifacts, scripts, and per-vertical planning references (objective/guidance split, `--guidance` flag, path-containment).
> **Sync resolution:** git auto-merged; both branches shared the review-planning-protocol commit history up to `1d1efd0f`, so most "both-sides" files were identical and incoming carried the superset. Generated-artifact consistency verified: `sync_codex_agents.py --scope project --check` reports all `.codex/agents/*.toml` and direct-mode role references up to date against the merged `agents/*.md` sources — no hand-edit drift.
> **Integration context:** None.
> **User decision:** None.
