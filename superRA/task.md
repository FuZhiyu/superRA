---
title: "superRA"
status: in-progress
depends_on: []
tags: []
created: 2026-05-23
---

## Objective

Develop and maintain the superRA repository: the PLAN -> IMPLEMENT -> INTEGRATE research workflow, its task-system tooling and dashboard, handoff artifacts, and the domain/utility skill library. Active workstreams nest as subtrees under this root.

The largest in-progress workstream is the handoff system — task tracking, planning artifacts, and human-readable visualization — whose integration history is recorded under ## Integration Notes and ## Sync Map below. Separate skill-development workstreams nest alongside it, including [zotero-skills](zotero-skills/task.md) (the MCP-free Zotero paper-reading skill plus the vendored `mistral-pdf-to-markdown` conversion skill).

## Conventions

Walk date: 2026-06-01.

`AGENTS.md` / `CLAUDE.md` at repo root is the contributor-facing authority for superRA internals. When modifying skills, hooks, agents, harness adapters, or internal docs, treat the work as skill creation: read owning files before editing, load `skill-creator` before editing `skills/*/SKILL.md`, keep changes focused, verify behavior rather than prose alone, preserve the README/user-facing vs AGENTS/internal separation, and apply the DRY + Necessity gate line by line. Ownership boundaries matter: workflow choreography belongs to `superplan` / `superimplement` / `superintegrate`; dispatch and reviewer-feedback adjudication belong to `agent-orchestration`; task anatomy belongs to `task-system/references/planning.md`; canonical role behavior belongs to `agents/implementer.md` and `agents/reviewer.md`; generated Codex/direct-mode artifacts must be regenerated from sources rather than hand-edited.

`README.md` is the user-facing product overview. It describes superRA as a PLAN -> IMPLEMENT -> INTEGRATE workflow with an implementer-reviewer pair, domain skills layered onto domain-neutral workflow phases, durable `superRA/` task files, and generated Codex named agents installed through `codex-superra-setup`. Keep product framing there; internal contributor rules stay in `AGENTS.md` / `CLAUDE.md`.

No additional `AGENTS.md`, `CLAUDE.md`, or `README.md` files were found under the touched implementation paths (`agents/`, `skills/task-system/`, `skills/superplan/`, `skills/using-superRA/`, `.codex/`, `superRA/review-planning-protocol/`). The walk intentionally did not load unrelated guidance under `skills/theory-modeling/`, `skills/writing/`, or `tests/claude-code/` because this task does not touch those directories.

## Integration Notes

- [task-system/cli-scripts](task-system/cli-scripts/task.md): researcher confirmed `better-handoff` as the Sync base for the packaged CLI redesign and noted incoming `postponed` status support must be preserved.
- [task-system/task-root-rename](task-system/task-root-rename/task.md): researcher confirmed `better-handoff` as the Sync base for the task-root rename integration.
- [task-system/task-root-rename](task-system/task-root-rename/task.md): synced with `better-handoff` through `981715dbb01eb99085d664778a121ce1a0d09f2c` (merge `104d96f3`, sync-map commit `35ba610e`, sync review `083c2d20`), post-sync integration reviewed APPROVED at `1bfed861`, and final checks passed (`uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/test_worktree_selector.py skills/task-system/scripts/tests/test_state_preservation.py`, 369 passed; `uv run pytest skills/codex-superra-setup/scripts/test_sync_codex_agents.py`, 6 passed; `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`; `python3 skills/task-system/scripts/task_check.py`; `git diff --check 981715dbb01eb99085d664778a121ce1a0d09f2c..HEAD`).
- [task-system/planning-redesign/planmd-sweep](task-system/planning-redesign/planmd-sweep/task.md): researcher chose manual integration checks rather than drift/static tests for the protocol-level results; integration base for this worktree is `better-handoff`.
- [task-system/planning-redesign/planmd-sweep](task-system/planning-redesign/planmd-sweep/task.md): synced with `better-handoff` through `19457675bf570a079bc960765a1832736cddf918`, sync reviewed, integration reviewed, task-tree consolidation completed, and targeted tests passed (`uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/tests/test_state_preservation.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py`, 205 passed).
- [task-system/dashboard/view-navigation](task-system/dashboard/view-navigation/task.md): synced onto `better-handoff` through `aad548e35d8309d386ce9a6f68b1f4c820502276` (merge `018a4f88`), sync reviewed APPROVED, integration reviewed APPROVED; pruned the now-dead `Task.has_child_dependency_graph()` and added card-flow tests (244 passing).
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): merged `plan/codex-task-hooks` into `better-handoff` from base `66693ccf`. Conflict resolution kept the base branch's committed template-backed standalone dashboard exporter and applied the incoming `updated` metadata removal through the shared task data layer and `templates/summary_bar.html`; unrelated in-flight subtree Share/export work was stashed out before verification and is not part of this sync. `.plan/**/task.md` frontmatter now omits `updated`. Verification passed on a clean tree: `uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/tests/test_state_preservation.py` (288 passed) and `python3 skills/task-system/scripts/task_check.py --plan-root .plan`.
- [review-planning-protocol](review-planning-protocol/task.md): synced with `better-handoff` through `75a86ccebd0ec37a0a9e904f0fd5b824752cf6f1` (merge `1d14b1f1`), integration review initially requested DRY/Necessity and task-root evidence fixes (`865f5ed0`), fixes landed in `2afde1d2`, and narrow integration re-review approved at `59d1ee4`. Final validation passed: `uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/test_worktree_selector.py skills/task-system/scripts/tests/test_state_preservation.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py` (379 passed), `python3 skills/task-system/scripts/task_check.py --plan-root superRA`, `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`, and `git diff --check 75a86ccebd0ec37a0a9e904f0fd5b824752cf6f1..HEAD`.
- [review-planning-protocol](review-planning-protocol/task.md) + [task-system/cli-scripts/path-containment](task-system/cli-scripts/path-containment/task.md): PR #29 follow-up pass (task 05 recursive-context-conventions + path-containment) integrated onto `better-handoff`. Synced through `876178e32d22ef2643424a584809d782fce4dde9` (merge `76c2a9af`, Sync Map commits `48e5143b`/`7f4535c4`, sync review APPROVED `ae132223`); post-sync integration review APPROVED with no blocking findings. Checks passed: `~/.venv/bin/python -m pytest skills/task-system/scripts/test_task_system.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py` (216 passed), `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`, `python3 skills/task-system/scripts/task_check.py --plan-root superRA`.
- [integ-workflow](task-system/agent-interface/integ-workflow/task.md) (integrate-cleanup redesign, merged into integ-workflow 2026-06-01 per the update-task lifecycle): researcher confirmed Sync base = local `better-handoff` (`876178e3`, anchors `PRE_SYNC_BASE_SHA=75a86cce`, `BASE_HEAD_SHA=876178e3`) and scoped this integration to the `integrate-cleanup` subtree only — `review-planning-protocol` remains already-closed carried work and is not re-reviewed. Protect uses manual prose-coherence checks rather than drift tests, consistent with the protocol-level-results decision recorded above for `planmd-sweep`: the integrate-cleanup changes are skill/reference text with no numeric results to drift-test, and both children were already correctness-reviewed (`0eeda33a`, `afa70c6f`). Synced onto `better-handoff` via clean disjoint merge `014dbcec` (sync review APPROVED `728ab399`); post-sync integration review found one MAJOR (the gate verdict was being written to a temporary `## Revision Notes` slot that warns on approved tasks and is matured/removed) — relocated to the root durable integration record, fixed `11e1214b`, narrow re-review APPROVED `4e6a67ac`. Integration closed; targeted suite green (247 passed, `task_check` clean, `git diff --check` clean over `1d1efd0f..HEAD`). Consolidation Gate verdict: **clean-enough — pass skipped** (frontier = parent + 2 children, the one cross-edge `results-maturation-home`→`consolidation-gate` is declared; zero `consolidation.md §When to Consolidate` symptoms present).
- [task-system/cli-scripts](task-system/cli-scripts/task.md): synced with `better-handoff` through `9ca25479f7cb588aec3d758f0bb27d66e4c8aded` (merge `136a19c9`, Sync Map commits `9427f1e0`/`47ae4e6a`, sync review APPROVED `f3c16b2b`), preserving incoming `postponed` status semantics and focused `superra task read` Context/tree behavior while keeping the packaged command surface (`superra task ...`, top-level `superra dashboard`) and no `superRA/serve` wrapper. Post-sync integration review APPROVED at `abe49e78`; exact `uvx --from ./skills/task-system` smoke passed after bumping `superra-task-system` to `0.1.2` to avoid stale cached local wheels. Final checks passed: `uv run pytest skills/task-system/scripts/test_task_system.py skills/task-system/scripts/test_dashboard.py skills/task-system/scripts/test_cli.py skills/codex-superra-setup/scripts/test_sync_codex_agents.py` (333 passed), `python3 skills/codex-superra-setup/scripts/sync_codex_agents.py --scope project --check`, `python3 skills/task-system/scripts/task_check.py --plan-root superRA`, and `git diff --check 9ca25479f7cb588aec3d758f0bb27d66e4c8aded..HEAD`. Consolidation Gate verdict: **clean-enough — pass skipped** (frontier = parent + 5 approved children; dependency DAG is a simple chain `uv-package` -> `unified-command-surface` -> `wrappers-and-hooks` -> `docs-tests-compat` with independent `path-containment`; zero `consolidation.md §When to Consolidate` symptoms present).
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): researcher confirmed local `better-handoff` as the Sync base for the `analysis/codex-task-hooks` branch before implementing/reviewing `04-docs-tests-and-compat`.
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): Sync re-check was a no-op on 2026-06-02: local `better-handoff` at `9ca25479f7cb588aec3d758f0bb27d66e4c8aded` was already an ancestor of `analysis/codex-task-hooks` (`c6aac1dc184e7798df9f8bfab482bd49cfab729f`), and the dispatched incoming range `9ca25479f7cb588aec3d758f0bb27d66e4c8aded..9ca25479f7cb588aec3d758f0bb27d66e4c8aded` was empty. No merge commit or Sync Map update was needed.
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): Protect decision, 2026-06-02 — existing regression and compatibility tests are sufficient protection for this non-numeric hook-packaging result set. Protected contracts are active decision-reminder removal, Codex task-system `PostToolUse` wiring, `updated` metadata removal, and docs/tests matching the installed Codex hook set.
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): Integrate closed on `analysis/codex-task-hooks` with integration review APPROVED (`2fade6f2`) over governing range `9ca25479f7cb588aec3d758f0bb27d66e4c8aded..HEAD`. Final validation passed: `python3 -m json.tool hooks/hooks-codex.json`, `bash tests/hooks/test-codex-hooks.sh` (14/14), `uv run pytest skills/task-system/scripts/test_task_system.py` (232 passed), `python3 skills/task-system/scripts/task_check.py --plan-root superRA`, and `git diff --check 9ca25479f7cb588aec3d758f0bb27d66e4c8aded..HEAD`. Consolidation Gate verdict: **clean-enough — pass skipped** (affected subtree = `task-system/codex-task-hooks` parent + four approved children; DAG has no codex-task-hooks dependency edges needing repair; zero `consolidation.md §When to Consolidate` symptoms present).
- [task-system/codex-task-hooks](task-system/codex-task-hooks/task.md): Documentation maturation review APPROVED on 2026-06-02. The parent task's `## Results` is the permanent reader-facing record; no figures/tables were needed, no stale review/revision sections remained in approved in-scope tasks, and spot-check validation passed (`python3 skills/task-system/scripts/task_check.py --plan-root superRA`, `python3 -m json.tool hooks/hooks-codex.json`, `bash tests/hooks/test-codex-hooks.sh` 14/14).
- [task-system/dashboard/serve-lifecycle](task-system/dashboard/serve-lifecycle/task.md): integration on local `better-handoff`, 2026-06-04. Researcher-set governing base = the pre-task commit `8c70b4e1` (tagged `pre-serve-lifecycle` as a recovery anchor); governing diff `8c70b4e1..HEAD`. **Sync = no-op** — `8c70b4e1` is already an ancestor of HEAD, no base advance, nothing to merge. **Protect decision:** existing regression tests are sufficient protection for this non-numeric infra result set (consistent with the `codex-task-hooks` Protect decision above). The feature shipped 23 tests (`TestIdleShutdownLifespan`, `TestRuntimeFileKeying`, `TestPidHelpers`, `TestBackgroundLaunch`) covering the protected contracts — background-by-default launch with bind-wait/failure surfacing, idle self-exit after the open-tab count stays zero, heartbeat pruning of dead connections, idempotent reuse on the recorded bound port, and `stop`; all tasks `approved`, so no new drift tests are scoped; full suite green (484 passed). Integration review over `8c70b4e1..HEAD` **APPROVED** (no blocking findings — minimum-net-diff, convention/utility fit, and Project Doc Audit all clean; verified the `pre-serve-lifecycle` tag resolves exactly to `8c70b4e1` = `5e1cb373^`). **Consolidation Gate verdict: clean-enough — pass skipped** (frontier = parent + 2 children on a simple `01-idle-shutdown` → `02-background-launch` chain; zero `consolidation.md §When to Consolidate` symptoms). Final-diff self-check: zero surviving hunks to prune over the governing range. Suite green (484 passed). Landed on the trunk `better-handoff` — no separate analysis branch to merge.
- [task-system/dashboard/mobile-ipad-ui](task-system/dashboard/mobile-ipad-ui/task.md): integration on local `better-handoff`, 2026-06-04. Researcher decisions folded into the objective: touch/responsive **adaptation only** (no restyle), touch sidebar **pinned-or-drawer** (never hover), phone search **reachable behind an icon**. **Sync:** `better-handoff` advanced from the `6a998a64` branch point to `6b6d4d30` (two `serve-lifecycle` integrate commits); merge `509744ef` was a clean **non-overlapping** sync — the incoming range touched only `superRA/task.md` (+1 line, disjoint from `base.html` / `test_dashboard.py` / the subtree), no conflicts, no results-bearing overlap. **Protect decision:** existing regression tests are sufficient protection for this non-numeric UI result set (consistent with the `serve-lifecycle` / `codex-task-hooks` decisions above) — the feature shipped touch-behavior regression tests (`TestTouchSidebar`, `TestTouchPolish`, `TestTouchPolishRendered`, plus cascade-specificity checks), red-green-verified; all tasks `approved`, so no new drift tests scoped; suite 365 → 383 (orchestrator local re-run: 381 passed + 2 Playwright-rendered tests that skip without a browser and ran green in the agent env). Integration review over `6b6d4d30..HEAD` **APPROVED** (`e7064253` is the only integration edit — a superseded-for-touch forward pointer on `view-navigation/a11y-responsive`; minimum-net-diff, theme-token/easing convention fit, Project Doc Audit, and task-file coherence all clean). **Consolidation Gate verdict: clean-enough — pass skipped** (added a tidy 2-leaf subtree `01-touch-sidebar` → `02-touch-polish` under the established `dashboard` parent; zero `consolidation.md §When to Consolidate` symptoms). **Document:** `## Results` already at reader-facing quality (per-leaf verification tables + figures, parent-task rollup as narrative home), confirmed coherent by integration review — no separate maturation pass. Landed on the trunk `better-handoff` via **squash-merge** of branch `dashboard-mobile-ui`; worktree retained for UI preview; nothing pushed to `origin` (44 commits behind local; local is the trunk).
- [task-system/cli-scripts/cli-source-resolution](task-system/cli-scripts/cli-source-resolution/task.md): integrated onto local `better-handoff` on 2026-06-07. Synced through `1a9f2916` (pre-sync base `78b19f8d`, merge `5de74de9`) — a clean disjoint merge of 7 overlapping task-system files (this branch's uvx-resolver / `superra wrapper` subcommand / `--root` cleanup vs incoming task-read fix, recursive placement ladder, update-task lifecycle, comment-surfacing CLI); no conflicts, both intents preserved, Sync Map verified. Combined post-sync integration review APPROVED at `cc21203a` after one round — MAJOR (stale `CLAUDE.md` wrapper-mechanism description) + MINOR (missing final-diff-self-check trail) fixed in `a51492c3`. The `wrapper_resolver.py` → `hooks/task-hook` byte-identity invariant and the generated wrapper's self-containedness hold post-merge; full `skills/task-system/scripts/` suite 569 passed; real-user path re-verified (generated wrapper run from a non-checkout dir resolves via `uvx` with no cwd `.venv`). Consolidation Gate verdict: **clean-enough — pass skipped** (cli-source-resolution is a well-placed sibling under `cli-scripts`, now 8/8; zero `consolidation.md §When to Consolidate` symptoms). Landed on the trunk `better-handoff` via fast-forward; nothing pushed to `origin`. **Superseded 2026-06-08:** the `uvx`-resolver approach recorded above was reopened (status reset to `not-started`) after verification found `uvx --from <dir>` serves stale builds (version-keyed cache) and the wrapper bootstrap was chicken-and-egg; cli-source-resolution is being reworked to a single `uv run --script` model (drops `pyproject.toml`) with the committed wrapper as the universal agent entry — see its ## Revision Notes. This record describes the prior integration only.
- [task-system/cli-scripts/cli-source-resolution](task-system/cli-scripts/cli-source-resolution/task.md) (uv run --script rework, 2026-06-08): reworked from the superseded `uvx` approach to a single `uv run --script` model. Dropped `skills/task-system/pyproject.toml` and the installable-package framing — PEP 723 inline metadata is now the single dependency source of truth; the run-line is routed by entry (`cli.py` / `plan_dashboard.py dashboard` / `task_hook.py`) with a `python3` fallback; the GitHub last-resort is a shallow clone; the committed `<task-root>/superra` wrapper is the universal agent entry (taught in `using-superRA §Task Interface`, so subagents never load `task-system`); and `superplan` creates the wrapper at tree-creation. Also fixed a latent lazy-`iterdir` crash on the bootstrap path under Python ≤3.12. Implemented + task-reviewed APPROVED (`f65317c6`); post-sync integration coherence review APPROVED at `b89d308d` (clean repo-wide stale-reference sweep over the `pyproject.toml` removal; CLAUDE.md local-dev + GitHub Actions template updated to `uv run --script`). Root-cause fixes verified empirically (no caller `.venv` from a project with a conflicting `.venv`; live-source edits reflected with no cache-bust) and the full `skills/task-system/scripts/` suite passed 596 / 2 skipped under both Python 3.12 and 3.14. Consolidation Gate: **clean-enough — pass skipped** (well-placed under `cli-scripts`, 8/8). A `--no-project` run-line hardening was considered and **rejected**: the PEP 723 block is the complete fix, and the `.venv` failure only recurs against a stale pre-block installed plugin, which reinstalling the plugin resolves — a transient condition not worth a permanent flag. Sync was a clean no-op fast-forward; landed on `better-handoff` via fast-forward; nothing pushed to `origin`.

## Sync Map

- [task-system/dashboard/github-artifact-action](task-system/dashboard/github-artifact-action/task.md): synced against local `better-handoff` on 2026-06-04. Feature-branch pre-sync merge base = `35feea5f`; artifact branch head = `73212f9b`; first incoming `better-handoff` head = `6b6d4d30`; final integration base head = `1069da5f`. Material code overlap was the dashboard command surface and docs/tests: incoming made `superra dashboard` background-by-default with `--foreground` and `superra dashboard stop`, while this branch added `superra dashboard artifact setup`. Resolution keeps both command paths, preserves browser auto-open for plain `superra dashboard` unless `--no-open` is passed, routes artifact setup without invoking the dashboard server, and preserves the base-side mobile/iPad dashboard integration record from `1069da5f`.
- [zotero-skills](zotero-skills/task.md): synced `origin/main` (PR #31, commit `5dfe928b`) into the trunk on 2026-06-05. Incoming is purely additive — the MCP-free `zotero-paper-reader` skill, the vendored `mistral-pdf-to-markdown` skill, their tests, and `README.md` / `skills/CATEGORIES.md` / `skills/using-superRA/SKILL.md` inventory entries. Two conflicts: `.gitignore` resolved by union (kept the trunk's `.plan/` + `**.pyc` entries, added `superRA/dashboard.html`); `superRA/task.md` was an add/add collision of two unrelated roots. Per researcher decision the trunk root was reframed from "Better Handoff" to the repo-level "superRA" root, and the incoming zotero workstream root + its 01–06 task subdirs were relocated as the `zotero-skills/` subtree (no content loss). Dashboard regenerated to reflect the new tree.
