---
title: "HTML Dashboard"
status: approved
depends_on:
  - core-data-layer
  - cli-scripts
---

## Objective

Provide a human-friendly dashboard over the `superRA/` task tree: recursive tree navigation with progressive disclosure, DAG (Mermaid) and kanban views, distinctive typography, a professional palette, and dark/light mode — and keep it hardened: task content escaped at one trust boundary, no blocking work on the event loop, no legacy module-global render state, dead rendering paths removed, and assets served locally.

### Context — hardening subtree (2026-07-19)

A full design review (backend `plan_dashboard.py` read line-by-line; frontend `base.html` + partials reviewed by a dispatched agent) produced the hardening work now folded into this task's `## Results`. Researcher decisions that governed it:

- **Scope:** all review findings are in scope — the six priority fixes plus the medium/minor items.
- **Escaping:** titles/section previews render HTML as literal text; markdown bodies keep HTML via DOMPurify.
- **Dead code:** the `/tree` + `/task/{path}` giant-tree subsystem is removed outright.
- **Assets:** all JS/CSS libraries served locally (vendor htmx/sse.js); Google Fonts stay CDN with system-font fallback.

Shared conventions for the subtree: the test-suite invocation and `uv run --script` conventions are in repo `CLAUDE.md` §Local Task-Tree CLI Development (standing context — run both the dashboard and full script suites before reporting); `vendor/` is hand-managed and re-fetched per `vendor/README.md`, never generated; no generated-from-spec artifacts are in scope for these tasks.

Dispatch note: the sibling `nonloopback-host-serve` task is `postponed` (researcher decision, 2026-08-02 — off the frontier until resumed). When it resumes it touches the background-supervisor region of `plan_dashboard.py`, so avoid running it in parallel with other work touching that file in one worktree, and coordinate ordering at dispatch time.

## Results

The dashboard is a **FastAPI live server** that renders `skills/task-tree/scripts/templates/base.html` plus htmx-swapped partials in a master-detail workspace, with a **standalone-mode render** of the same template for static export. `base.html` now carries page structure and a small Jinja config block only; its ~1,760 CSS and ~3,000 JS lines live in `dashboard.css`/`dashboard.js`, which the live server serves cacheably (`GET /static/{name}`, SHA-256 ETag + 304) and the standalone export inlines. The server-side machinery (routes, SSE broadcast, watcher lifecycle, idle monitor, Jinja render helpers, standalone build, CLI, background supervisor) lives in `skills/task-tree/scripts/plan_dashboard.py`; the standalone render threads its tree/index/project-root state explicitly through the per-worktree `WorktreeState` seam, with no module-global render state left.

What shipped, past the original single-file static HTML the objective first scoped:

- **View navigation** — the master-detail workspace, sidebar tree, breadcrumb, DAG (Mermaid) and kanban surfaces, and client routing, with exactly one row-rendering path (`nav_node.html`) after the dead pre-master-detail giant-tree subsystem (`GET /tree`, `GET /task/{path}`, and the `task_node.html`/`task_children.html` templates) was removed. The children dependency panel is built from a structured `GET /api/children-graph` JSON payload (nodes + edges taken straight off `Task` data), not regex-parsed mermaid source — so the status→color mapping lives in exactly one place and a task title carrying a literal ` --> ` can no longer corrupt the panel.
- **Unified static export** — one template (`base.html`) drives both the live server and the static export via a `window.STANDALONE` branch, replacing the separate static generator.
- **Self-contained, offline export** — the static export base64-embeds figures and inlines every render library (markdown-it, KaTeX, highlight.js, DOMPurify, texmath, htmx, sse.js) alongside the extracted `dashboard.css`/`dashboard.js`, so a downloaded file renders fully offline; the only network reference is Google Fonts (system-font fallback).
- **Offline-functional live mode** — live mode also loads every render library from the server's own `/static/…` route out of the hand-managed `vendor/` bundle rather than a CDN (htmx and sse.js were vendored to close the last two gaps); a blocked network degrades only typography, never rendering or SSE.
- **Multi-worktree serving** — one server/port per repo resolves any worktree per request via `?wt=<wt_id>`; switching is client navigation, not a server-wide swap.
- **Serve lifecycle** — background-by-default launch with idempotent reuse, idle self-exit, `stop`, and a loopback-default `--host` bind.

Task content is escaped at a single server-side trust boundary: Jinja autoescape is on, titles and section previews render HTML as literal text, and markdown bodies keep full HTML through the client DOMPurify gate. The kanban card wires clicks through a delegated `data-path` handler instead of an interpolated inline `onclick`, and comment-anchor selectors use `CSS.escape` so a `"` in a `##` header no longer aborts comment loading. Client DOM writes continue to go through `textContent`/`escapeHtml` helpers and controlled markdown-it rewrites.

Preserved throughout: distinctive typography (Source Serif 4 + IBM Plex Mono via Google Fonts CDN), the warm parchment/ink palette with muted status tints, dark/light mode, progressive disclosure, and the DAG/kanban views.

### Server robustness

Blocking route work runs off the event loop: `/export`, `/api/worktrees`, `/api/comments/summary`, and the worktree cache-miss path of `resolve_worktree` hand their git-subprocess, tree-walk, render, and base64-encoding cores to `asyncio.to_thread`, while every mutation of shared server state (`_worktree_cache`, client sets, watcher maps) stays on the loop thread — so SSE heartbeats and cheap requests stay responsive during a slow export ([plan_dashboard.py](../../../skills/task-tree/scripts/plan_dashboard.py)).

The server fails loudly and keeps its connection bookkeeping truthful. A `task.md` that fails to re-parse during a watcher rebuild logs to stderr and stamps a `parse_error` state — a `badge-error` badge plus a `data-parse-error` attribute on the node ([nav_node.html](../../../skills/task-tree/scripts/templates/nav_node.html)) — instead of silently serving the last-good parse. A slow SSE client whose queue overflows receives a `_QUEUE_CLOSE` sentinel that ends its stream through the normal disconnect path, restoring the invariant the idle monitor depends on: registered queues == open connections. The root-or-children snippet that was duplicated inline is now one shared precompiled `Template`, and the nav-node render helper uses a precompiled template instead of a per-call `env.from_string`.

### Shutdown lifecycle protection

Watcher teardown is bounded under repeated caller cancellation: it gets a cooperative grace period, then a bounded forced-cancellation phase, and finally a process watchdog for a cancellation-suppressing watcher. The watchdog is restricted to a standalone `plan_dashboard.py` process running as `__main__` on its main thread, and late watcher completion disarms it ([plan_dashboard.py:615-745](../../../skills/task-tree/scripts/plan_dashboard.py#L615-L745)).

Permanent regressions cover both the focused cancellation bounds and two real detached-process cycles with eight concurrent abrupt SSE resets; each process cycle verifies the native watcher returns, the child exits, and its port closes before relaunch ([test_dashboard.py:1314-1485](../../../skills/task-tree/scripts/test_dashboard.py#L1314-L1485), [test_dashboard.py:5449](../../../skills/task-tree/scripts/test_dashboard.py#L5449)).

### Reconnect freshness

When a worktree watcher starts after an interval with no connected client, the
server rebuilds that worktree's cached task state before watching for new
changes, then sends a worktree-scoped `full-reload` to the reconnecting SSE
client. Because the client queue is registered before watcher startup, edits
made while the watcher was stopped are visible immediately on reconnect; an
ensure call against an already-live watcher emits no duplicate refresh
([plan_dashboard.py:579-599](../../../skills/task-tree/scripts/plan_dashboard.py#L579-L599)).

Permanent regressions cover the offline edit, refreshed cache, reconnect-stream
notification, duplicate suppression, and cross-worktree broadcast isolation
([test_dashboard.py:1162-1180](../../../skills/task-tree/scripts/test_dashboard.py#L1162-L1180),
[test_dashboard.py:1225-1312](../../../skills/task-tree/scripts/test_dashboard.py#L1225-L1312)).

### Opening files, and naming the tab

The dashboard hands files to the machine it runs on instead of only firing a `vscode://` URI, which names one editor and cannot express a workspace. A loopback-gated [`POST /api/open`](../../../skills/task-tree/scripts/plan_dashboard.py#L1413) backs five surfaces: the card-head **Open** button (the OS default application for the type), the header **VS Code** button (the active task's file, inside the window already holding *its* worktree), body file links, attachment links in a rendered task body, and the artifact pane's **Open** button. One delegated handler serves all five — a plain left-click calls the route, while any modifier or middle click falls through to the element's own `vscode://` or `/api/artifact` href, so the browser's open-elsewhere gestures survive and the `#L123` line anchor still reaches VS Code.

Every file surface therefore opens the same way. The artifact pane's button was the last that did not: it read **Open raw** and only ever handed the file to a browser tab over `/api/artifact`, an affordance the task-file button has no equivalent of. It is now plain **Open**, carrying the route address for the plain click and the same `/api/artifact` href for a modifier click ([`buildArtifactPreviewHead`](../../../skills/task-tree/scripts/templates/dashboard.js#L1390)) — the pair an attachment link in a task body already carried. Every project-root-relative address the page sends the route now comes from one composition helper, [`taskRelOpenPath`](../../../skills/task-tree/scripts/templates/dashboard.js#L1103) — `taskFileOpenPath`, the body attachment link, and the pane button all delegate to it — so no two surfaces can drift apart on a `--root`, a nested tree, or a rootless forest. Outside the gate — doc-mode, standalone, an off-loopback bind — the button keeps the href alone and degrades exactly as the task-file button degrades to its `vscode://` or GitHub link.

The gate is the whole remote story: [`_local_open_enabled`](../../../skills/task-tree/scripts/plan_dashboard.py#L1368-L1376) requires a loopback bind and non-doc-mode, so an off-loopback `--host`, doc-mode, and standalone all render the page flag false and every control keeps the deep link it emitted before. Beyond the gate the route required a `Host`-authority check to be worth anything: same-origin and content-type checks alone fall to DNS rebinding, which a review pass demonstrated live against a running server before the fix. Paths resolve under the project root only and the target must be `is_file()`, so a repo-contained `.app` bundle is not openable, and the single launch point spawns an argv list with `shell=False`. Accepted gap, decided at planning: an SSH port-forward is indistinguishable from a local request, so an open over a tunnel runs on the server's machine — no detection heuristic, per-browser setting, or Remote-SSH URI form was added.

A refused or failed open raises a transient in-page notice ([`showOpenError`](../../../skills/task-tree/scripts/templates/dashboard.js#L1114), [.open-toast](../../../skills/task-tree/scripts/templates/dashboard.css#L1127-L1146)). A successful open's only visible result is on the researcher's desktop, so without the notice a refusal is indistinguishable from a dead button. The controls themselves were pulled into one family: `.vscode-btn` became [`.open-btn`](../../../skills/task-tree/scripts/templates/dashboard.css#L1097-L1122) now that it also styles a native-open control, the rename carrying through the doc-mode hide rule ([dashboard.css:150](../../../skills/task-tree/scripts/templates/dashboard.css#L150)), the `pointer: coarse` tap-highlight list ([dashboard.css:901](../../../skills/task-tree/scripts/templates/dashboard.css#L901)), the `#worktree-open-btn { margin-left: 0 }` override ([dashboard.css:313](../../../skills/task-tree/scripts/templates/dashboard.css#L313)), and the header element ([base.html:70](../../../skills/task-tree/scripts/templates/base.html#L70)). Its off-palette `#007acc` hover gave way to the `--accent-soft` / `--accent` pair the neighbouring `.share-btn` already uses, and the solid VS Code wordmark to the outline [`EDITOR_ICON` / `OPEN_ICON`](../../../skills/task-tree/scripts/templates/dashboard.js#L1066-L1077) pair on a shared 2px stroke — which is what lets the card-head button carry no editor branding when it opens in the machine's default application.

The browser tab reads `<active page> · <where it lives>` — the worktree branch in a live dashboard, the site or export name in doc-mode and standalone — so tabs of different worktrees, and of different pages in one worktree, are tellable apart. The page half is whatever the main panel shows: the active task's title, or the attachment's name while the artifact pane holds one, cleared back to the tree's own name when either fails to load, so the tab never names something the panel is not showing. The halves join only when they differ, so a task titled the same as its branch does not read `main · main` and an export root does not double ([dashboard.js:931](../../../skills/task-tree/scripts/templates/dashboard.js#L931)). A cold deep link can render the card before the sidebar row carrying the real title lands; [`patchTabTitleWhenReady`](../../../skills/task-tree/scripts/templates/dashboard.js#L2079) re-reads the title once that tick's sidebar update settles rather than stranding the tab on the path slug. Mode decides only the second half, never whether the title tracks the page, which is what gives the published docs site per-page titles ([`refreshTabTitle`](../../../skills/task-tree/scripts/templates/dashboard.js#L922-L932)).

For adopters the behavior is documented in [the quickstart](../../../docs/site/02-quickstart/task.md#L23) and, with `SUPERRA_EDITOR` and the bind/mode conditions, in [internals.md §Dashboard](../../../skills/task-tree/references/internals.md#L232); both behaviors ship in [0.3.6](../../../RELEASE-NOTES.md#L35).

Permanent regressions pin the route's refusals (off-loopback, doc-mode, wrong content type, cross-site, foreign `Host`, traversal, absolute path, directory, missing file, bad target), both open paths' exact argv, the no-editor-CLI fallback, the `SUPERRA_EDITOR` override, and the decoded-path contract ([test_dashboard.py:3354](../../../skills/task-tree/scripts/test_dashboard.py#L3354)); and the title composition in live, doc-mode, and standalone, including the branch/directory-name fallback, the pre-fetch state, and both card-error branches ([test_dashboard.py:3170](../../../skills/task-tree/scripts/test_dashboard.py#L3170), [test_dashboard.py:3261](../../../skills/task-tree/scripts/test_dashboard.py#L3261)). The artifact pane's button is pinned twice: by source assertion that it offers no browser-only open the task-file button lacks ([test_dashboard.py:3659](../../../skills/task-tree/scripts/test_dashboard.py#L3659)), and by a real-browser regression that clicks it against a live loopback server and asserts the spawned argv names the attachment on disk while the page stays on its artifact route ([test_artifact_ui.py:486](../../../skills/task-tree/scripts/test_artifact_ui.py#L486)).

### Releases

The dashboard hardening and worktree image/reconnect fixes are documented in the dated [0.3.3 release notes](../../../RELEASE-NOTES.md#L110), and every maintained Claude, marketplace, and Codex manifest agreed at `0.3.3` when that release shipped. The release workflow that section was written against still stands: it runs `scripts/bump-version.sh --check` before reading or using the release version, requires exactly one non-empty section for that version directly below `Unreleased`, and runs its release job only on `refs/heads/main` — so the workflow remains the sole owner of idempotent tag and release creation after merge ([release.yml:32](../../../.github/workflows/release.yml#L32), [release.yml:23](../../../.github/workflows/release.yml#L23), [release.yml:45](../../../.github/workflows/release.yml#L45)).

Local file open and the task-scoped tab title cut [0.3.6](../../../RELEASE-NOTES.md#L35), a release of their own rather than an addition to the already-dated `0.3.5` — they are a separate update from the attachment subsystem that release carried. All three manifests are bumped and in sync at `0.3.6`, and the workflow's own extraction logic was run against `RELEASE-NOTES.md` locally: exactly one `## [0.3.6]` section, directly below an empty `Unreleased`, non-empty.

**Design debt (recorded for a future extraction pass):** `skills/task-tree/scripts/plan_dashboard.py` is ~2,740 lines mixing several concerns. The legacy module-global render state and the `/export` snapshot/restore dance are gone — the standalone render owns its state via `WorktreeState` — and the ~1,760 CSS / ~3,000 JS lines that used to inflate `base.html` now live in separate cacheable files. What remains: the background supervisor (~300 lines of PID/daemon logic) and the standalone build (~340 lines) are FastAPI-decoupled, clean extraction candidates following the `skills/task-tree/scripts/dashboard_artifact_workflow.py` precedent.

**Verification.** The dashboard module passes 308 tests with two dependency warnings, and the full task-tree script suite passes 731 tests with four expected/dependency warnings (`uv run --with pytest --with pyyaml --with fastapi --with jinja2 --with 'uvicorn[standard]' --with watchfiles --with httpx python -m pytest skills/task-tree/scripts`). The consolidated tree and dashboard DAG render correctly, `task check` reports no issues, the durable task passes the Markdown checker, and `git diff --check` is clean. Release verification also confirms synchronized `0.3.3` manifests, a clean version audit, valid workflow YAML and main-ref gating, and exactly one non-empty `0.3.3` notes section directly below `Unreleased`. Each hardening child's merged diff traced cleanly to its objective with no scope-ambiguous hunks, no `skills/*`/`agents/*` instruction edits, and `vendor/` left hand-managed per its `README.md`; per-task red/green and byte-identical-export checks are recorded in this task's git history.

Those counts predate the local-open and tab-title work. The merged tree — this work plus the incoming attachment subsystem — passes 795 tests across `skills/task-tree/scripts` with the same four expected warnings, on the same command, run at Integrate. Unifying the artifact pane's Open with the task-file button added one browser regression and one source assertion: 797 across `skills/task-tree/scripts` on the same command with `--with playwright` added so the browser suite runs rather than skips, green on three consecutive sequential runs. Run two of those suites concurrently and the pre-existing browser tests time out on their 5s Playwright waits — a load artifact of the shared machine, not a code path.

**Live evidence for the two claims the suite cannot reach, both established pre-merge on code the merge did not touch.** *Editor window targeting* was driven against the real `code` CLI: two worktrees of this repo open in two VS Code windows beside two unrelated ones, and `code --status` before and after each open showed the file landing in the window already holding its own worktree rather than the focused one, with no window spawned — four windows throughout, across both directions. The degraded case was exercised too: against a window holding a *subfolder* rather than the worktree root, the route opened a correctly-rooted new window and left the subfolder window alone, so the failure mode is an extra window, never the wrong one. A `SUPERRA_EDITOR` recording script separately captured the exact `code <worktree> <that worktree's file>` pairing, which also exercised the env override end to end. The suite can only pin the argv ([test_dashboard.py:3426](../../../skills/task-tree/scripts/test_dashboard.py#L3426)); that `code <folder> <file>` reuses the right window is a property of VS Code, and only that live pass established it. *DNS rebinding* was likewise demonstrated live against a running server before the `Host`-authority check existed, which is what showed the same-origin and content-type checks to be insufficient on their own.

**What the green suite still does not reach.** The route's refusals and both open paths run end to end through ASGI, and the tab-title composition runs under node against the extracted functions. One click path is now driven in a real browser end to end — the artifact pane's **Open**, through the delegated handler, the `fetch`, and the server spawn. The rest are not: the card-head button, the header VS Code button, and body file links are pinned by source assertion against `dashboard.js`, as is the artifact pane's tab title, and no pass has rendered the failure toast in a browser.
