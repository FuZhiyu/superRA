# Attachment Markdown Preview

This retained example makes the normal dashboard reading pane easy to inspect
without adding a permanent project document. It links to the neighboring
[Python attachment](renderer-example.py).

## Renderer checks

- Inline math: $f(x) = x^2 + 1$
- Syntax highlighting:

```python
def squared_plus_one(value: float) -> float:
    return value**2 + 1
```

The same expression as a display equation:

$$
f(x) = x^2 + 1
$$

| Input | Output |
|------:|-------:|
|     2 |      5 |
|     3 |     10 |
